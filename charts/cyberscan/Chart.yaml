apiVersion: v2
name: cyberscan
description: Cyberscan — OSS web vulnerability scanner (v0.1 stub chart).
type: application
version: 0.1.0
appVersion: "0.1.0"
maintainers:
  - name: cyberscan contributors
# Postgres / redis / minio all ship as chart-local templates using the
# upstream-official images (postgres:18-alpine, redis:8-alpine,
# minio/minio:RELEASE.*). The Bitnami subcharts were dropped in 2026-05
# after Bitnami archived their public image tags to `bitnamilegacy/*`
# and started failing the chart's own NOTES.txt image-allowlist check.
dependencies: []
